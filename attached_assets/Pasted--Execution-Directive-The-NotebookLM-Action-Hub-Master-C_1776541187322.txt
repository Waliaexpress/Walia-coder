**Execution Directive: The NotebookLM Action Hub & Master Context Menu**

**Status Assessment:** We must finalize the Prompt Box UI. The Context Attachment trigger MUST be positioned immediately next to the 'Generate' button to form a unified Action Hub. Furthermore, the dropdown menu must be expanded to include the exact NotebookLM and Drive integrations provided in the design specs.

**As Lead UI/UX Engineer, execute this specific layout and menu mutation:**

**1. The Action Hub Layout (Inside the Textarea Wrapper):**
- Create a dedicated `div` at the bottom of the prompt input wrapper: `<div className="flex items-center justify-between w-full pt-3 mt-2 border-t border-gray-800">`
- **Left Side:** Leave empty or add subtle tooltip text.
- **Right Side (The Action Hub):** Create a tightly grouped flex container: `<div className="flex items-center gap-2 relative">`

**2. The Unified Button Group:**
Inside the Right Side container, place these elements exactly in this order:
   1. **The Context Trigger:** A `+` button or Paperclip icon. Style it as: `text-gray-400 hover:text-white p-2 rounded-md hover:bg-gray-800 transition-colors`.
   2. **The Generate Button:** The primary action button (`bg-blue-600 hover:bg-blue-500 text-white px-4 py-2 rounded-md font-medium`). 
   *(Resulting layout visually: `[ + ] [ Generate ]`)*

**3. The Expanded Master Context Menu (NotebookLM Style):**
- When the Context Trigger is clicked, open a popover anchored to it (`absolute bottom-full right-0 mb-2 w-56 z-50`).
- **Theme:** Strict Dark Mode (`bg-[#2b2d31] border border-gray-700/50 rounded-xl shadow-2xl py-2`).
- **Render these exact items in this order** (use appropriate Lucide/Heroicons):
  1. `[Paperclip Icon]` **Upload files** (Triggers local file input)
  2. `[Drive Icon]` **Add from Drive**
  3. `[Photos Icon]` **Photos**
  4. `[Code < > Icon]` **Import code**
  5. `[Waves/Network Icon]` **NotebookLM**
  6. `[Figma Icon]` **Attach a Figma design**
  7. `<div class="flex items-center justify-between w-full"><span>[Shapes Icon] Use a skill</span> <ChevronRight size={14}/></div>`

**4. Menu Styling:**
- Every item must use this exact class structure: `flex items-center gap-3 px-4 py-2 hover:bg-gray-700/50 text-gray-200 cursor-pointer text-[14px] transition-colors`.

**Action Required:**
Refactor the prompt input layout to group the attachment trigger and the Generate button side-by-side using Flexbox. Build the expanded Master Context Menu with all 7 items exactly as listed. Ensure the menu pops UP from the trigger without clipping off the screen.